1. **修改账户列表页面 (`app/accounts/page.tsx`)**：
   * 在遍历账户列表时，计算每个账户的总余额。
   * 总余额计算公式：`当前账户余额 + 所有子账户余额之和`。
   * 将卡片上显示的“当前余额”更新为计算出的总余额。

这样，当您查看父账户（如“招商银行”）时，看到的余额将包含其下属子账户（如“朝朝宝”）的金额。子账户列表依然会在卡片底部展示，方便查看明细。