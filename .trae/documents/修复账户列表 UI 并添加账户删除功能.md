1. **修复账户列表 UI (`app/accounts/page.tsx`)**：

   * 将右上角的货币单位（如 "CNY"）移动到余额数字的旁边，从而腾出右上角空间给编辑按钮，解决重叠问题。

2. **实现账户删除功能 (`components/account-form.tsx`)**：

   * 在账户编辑表单底部添加“删除账户”区域。

   * 使用 `Dialog` 组件实现二次确认弹窗。

   * 在弹窗中要求用户输入账户名称，只有输入正确时才允许点击删除按钮。

   * 调用 `deleteAccount` Server Action 执行删除，并处理可能出现的错误（如账户有交易记录无法删除）。

